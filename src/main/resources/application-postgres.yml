spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agent_ready_api
    username: agent_user
    password: agent_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
